server:
  port: 8761

logging:
  level:
    web: debug

spring:
  application:
    name: eureka-server

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/
